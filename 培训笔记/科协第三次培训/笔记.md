### 变量
字节是最小的存储单位.
C语言能操作的最小单位是一个字节.
数据类型决定了(            )
强类型语言必须事前定制数据类型,可以是程序的运行更加迅速,弱类型语言则不需要.编译器会根据赋值情况自动推演出类型.
sizeof()属于一个操作符.如果其后跟的是变量名称,则()可以省略,如果是变量类型则不可省略.
一个正常数据类型和无符号类型表示的最大数据范围扩大了一倍.
整数在内存中的存储类型是二进制.
整数的原码,反码和补码都是一样的,负数
两个符号相同的数加减是用的是原码.
补码可以简化电路.
<<1相当于乘以2.